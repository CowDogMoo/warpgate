{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Test Image Builder",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceRoot}",
      "args": [
        "imageBuilder",
        "-b",
        "atomic-red-team",
        "-p",
        "${env:HOME}/security/ansible-collection-arsenal",
        "-t",
        "$(op item get 'CowDogMoo/warpgate BOT TOKEN' --fields token)"
      ]
    }
  ]
}
