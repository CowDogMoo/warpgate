---
blueprint:
  max_retries: 3
  name: windows
  packer_templates:
    image_values:
      name: Windows_Server
      version: 2019
    # Provisioning user
    user: Administrator
    # AMI specific configurations
    ami:
      instance_type: t3.micro
      region: us-west-1
    # Tags for created odysseys
    tag:
      name: windows-server
      version: 2019
