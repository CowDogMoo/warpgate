---
blueprint:
  name: atomic-red-team

packer_templates:
  - name: atomic-red-team.pkr.hcl
    base:
      name: ubuntu
      version: jammy
    tag:
      name: l50/atomic-red-team
      version: latest
    user: ubuntu

# AMI specific configurations
ami:
  instance_type: t3.medium
  region: us-east-1

container:
  workdir: /root/AtomicRedTeam
  registry:
    server: ghcr.io
    username: l50
