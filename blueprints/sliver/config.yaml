---
blueprint:
  name: sliver

packer_templates:
  - name: sliver.pkr.hcl
    base:
      name: ubuntu
      version: latest
    tag:
      name: l50/sliver
      version: latest

container:
  workdir: /home/sliver
  # entrypoint: "/run/docker-entrypoint.sh ; sh"
  user: sliver
  registry:
    server: ghcr.io
    username: l50
