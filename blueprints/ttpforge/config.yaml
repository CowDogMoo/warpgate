---
blueprint:
  name: ttpforge

packer_templates:
  - name: ttpforge.pkr.hcl
    base:
      name: ubuntu
      version: jammy
    tag:
      name: l50/ttpforge
      version: latest
    user: ubuntu

# AMI specific configurations
ami:
  instance_type: t3.medium
  region: us-east-1

# Container specific configurations
container:
  workdir: /home/ubuntu
  registry:
    server: ghcr.io
    username: l50
